<nav>
    
    <div class="logoArea"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1770 1000">   <circle cx="500" cy="500" r="500"/>   <ellipse ry="475" rx="250" cy="501" cx="1296"/>   <ellipse cx="1682" cy="502" rx="88" ry="424"/><span><h1>Medium</h1></span></svg></div>
    
    <div class="hamburger">
        <a href="#" class="toggle-button">
            <span class="bar"></span>
            <span class="bar"></span>
            <span class="bar"></span>
            <span class="bar"></span>
        </a>
    </div>


    <div class="navbar-links">
        <ul>
              <!-- user avatar -->
            <% if (user) { %>
                <div class="user-avatar">
                    <p class="navbar-item"><%= user.username %></p>
                    <div class="avatar-img">
                        <img src="<%= user.avatar%>" width="50" height="50">
                    </div>
                </div>
            <% } %>
            <!-- routes -->
            <% if (routes) { %> 
                <% routes.forEach((route) => { %>
                <li><a class="navbar-item" href="<%= route.href %>"> <%= route.title %> </a></li>
                <% }) %> 
            <% } %>
        </ul>
    </div>
    
</nav>



